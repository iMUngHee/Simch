[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
dev = "uvicorn main:app --reload --port 8080"

[packages]
fastapi = {extras = ["all"], version = "*"}

[dev-packages]
autopep8 = "*"

[requires]
python_version = "3.10"
